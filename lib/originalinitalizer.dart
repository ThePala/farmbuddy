import 'package:flutter/material.dart';
import 'package:farmbuddy/main.dart';

void main() {
  runApp(MaterialApp(
    // routes: {
    //   '/': (context) => LoginApp(),
    //   '/splash': (context) => SplashScreen(), // Add the splash screen route
    // },
    home: MyApp(), // Set the SplashScreen as the initial route
  ));
}

